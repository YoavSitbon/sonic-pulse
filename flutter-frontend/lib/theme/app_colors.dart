import 'package:flutter/material.dart';

/// Nocturnal Resonance design system — all color constants.
class AppColors {
  AppColors._();

  // ── Core background / surface ───────────────────────────────────
  static const Color background           = Color(0xFF0F131C);
  static const Color surface              = Color(0xFF131824);
  static const Color surfaceVariant       = Color(0xFF1C2232);
  static const Color surfaceContainer     = Color(0xFF161C2A);
  static const Color surfaceContainerLow  = Color(0xFF131825);
  static const Color surfaceContainerHigh = Color(0xFF1D2438);
  static const Color surfaceContainerHighest = Color(0xFF232C42);
  static const Color surfaceContainerLowest  = Color(0xFF0D1018);

  // ── Primary — periwinkle lavender ──────────────────────────────
  static const Color primary              = Color(0xFFC0C1FF);
  static const Color primaryContainer     = Color(0xFF5B5CEA);
  static const Color primaryFixedDim      = Color(0xFF9899E8);
  static const Color onPrimaryContainer   = Color(0xFFF0F0FF);

  // ── Secondary — electric cyan ───────────────────────────────────
  static const Color secondary            = Color(0xFF4CD7F6);
  static const Color secondaryContainer   = Color(0xFF0E7EA3);
  static const Color secondaryFixedDim    = Color(0xFF2BBDD8);
  static const Color onSecondary          = Color(0xFF00202B);

  // ── Tertiary — soft violet ──────────────────────────────────────
  static const Color tertiary             = Color(0xFFDDB7FF);
  static const Color tertiaryContainer    = Color(0xFF6B3FA0);

  // ── On-surface text / icon ──────────────────────────────────────
  static const Color onSurface           = Color(0xFFECEDFF);
  static const Color onSurfaceVariant    = Color(0xFF8E97B8);

  // ── Outlines / dividers ─────────────────────────────────────────
  static const Color outline             = Color(0xFF4B5270);
  static const Color outlineVariant      = Color(0xFF2E3655);
}
